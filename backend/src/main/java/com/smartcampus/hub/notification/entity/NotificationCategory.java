package com.smartcampus.hub.notification.entity;

/**
 * High-level grouping for notification filtering in the UI.
 *
 * <p>Maps to the category tabs shown in the notification dropdown / page:
 * ALL | BOOKING | TICKET | USER | SYSTEM
 */
public enum NotificationCategory {
    BOOKING,
    TICKET,
    USER,
    SYSTEM
}
